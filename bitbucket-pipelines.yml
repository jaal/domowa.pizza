image: node:14-buster

pipelines:
  default:
      - step:
          name: Build
          caches:
            - node
          script:
            - apt-get update && apt install -y build-essential
            - SHARP_IGNORE_GLOBAL_LIBVIPS=true npm ci
            - npm run-script build
      - step:
          name: Deploy
          script:
            - pipe: atlassian/rsync-deploy:0.6.0
              variables:
                USER:   'technologie'
                SERVER: 'domowa.pizza'
                REMOTE_PATH: 'web/dev_domowa_pizza/'
                LOCAL_PATH: './public/*'
